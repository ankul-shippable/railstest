language: ruby

# env:
#   global:
#     - SHIPPABLE_BUNDLER_VERSION=2.0.0

rvm:
  - 2.6

build:
  ci:
    - ruby -v
    - gem install bundler
    - bundle install 
    - rake test test/myproject_more_test.rb
    - sudo apt-get install tree
    - tree
    - cat test/reports/TEST-MyprojectMoreTest.xml
    - cat test/reports/TEST-MyprojectTest.xml
