language: ruby

# env:
#   global:
#     - SHIPPABLE_BUNDLER_VERSION=2.0.0

rvm:
  - 2.5

build:
  ci:
    - ruby -v
    - gem install bundler
    - gem install rake
    - gem install minitest
    - gem install minitest-reporters
    - bundle install
    - rake test --trace
